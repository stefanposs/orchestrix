[project]
name = "orchestrix-core"
version = "0.1.0"
description = "Core abstractions for Orchestrix"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["orchestrix"]
