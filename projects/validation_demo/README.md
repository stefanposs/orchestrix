# Validation Demo

Input validation and guard clauses for domain events.

## Running

```bash
uv run python -m bases.orchestrix.validation_demo.demo_validation
```

## Architecture
All logic lives in `bases/orchestrix/validation_demo/`. This project assembly is a thin deployment wrapper following the Polylith pattern.
