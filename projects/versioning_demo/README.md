# Versioning Demo

Event schema evolution with version-aware handlers and upcasters.

## Running

```bash
uv run python -m bases.orchestrix.versioning_demo.demo_versioning
```

## Architecture
All logic lives in `bases/orchestrix/versioning_demo/`. This project assembly is a thin deployment wrapper following the Polylith pattern.
