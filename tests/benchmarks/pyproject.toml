# Benchmark-specific ruff configuration
# Benchmarks use pytest fixtures without type annotations (standard pattern)
[tool.ruff]
extend-ignore = ["ANN001", "ANN201"]  # Missing type annotations in benchmark tests
